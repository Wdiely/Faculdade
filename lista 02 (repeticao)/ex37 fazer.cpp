#include<iostream>
using namespace std;

void academia(){
	float alto=0, baixo=0, magro=0, gordo=0;
	int cod;
	
	do{
		cout << "Codigo cliente:";
		cin >> codigo;
		cout << "Informe sua altura:";
		cin >> altura;
		cout << "Informe seu peso:";
		cin >> peso;
		if()
	}while(codigo > 0);
	}
}